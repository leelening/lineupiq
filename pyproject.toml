[project]
name = "lineupiq"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12,<3.14"
dependencies = [
    "pandas",
    "mip",
    "tabulate",
    "requests",
]

[tool.uv]
environments = ["sys_platform == 'linux'"]
